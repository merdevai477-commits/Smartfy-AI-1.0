{
  "name": "smartfy-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install --legacy-peer-deps && npm run build",
    "start": "cd backend && npm start",
    "vercel-build": "cd backend && npm install --legacy-peer-deps && npm run build",
    "install": "echo 'Installing dependencies...'"
  }
}
